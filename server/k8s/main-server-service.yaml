apiVersion: v1
kind: Service
metadata:
  name: main-server
  annotations:
    cloud.google.com/app-protocols: '{"http2":"HTTP2"}'
spec:
  selector:
    app: main-server
  type: NodePort
  ports:
  - name: http2
    port: 3000